export default function Mentions() {
  return (
    <section className="card text-center">
      <h2 className="[font-family:var(--font-playfair)]" style={{fontSize:'clamp(1.4rem,3vw,2rem)'}}>La Maison Kaura — Present, but silent.</h2>
      <p className="opacity-75 max-w-2xl mx-auto mt-3 [font-family:var(--font-inter)]">
        Typographies : <em>Playfair Display</em>  & <em>Inter</em>. Fond #0a0a0a. Golden accents reduced. Only One action : <em> Elevate </em>.
      </p>
    </section>
  );
}